sp {learn-condition*propose*
   (state <s> ^name learn-condition
              ^topstate.current-game.learning <l>)
   (<l> ^type condition ^parameter <type>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name find-action
        ^category svs) # for now determine better or new category)
}