# Fetch the category for the given word from memory
sp {find-object*propose*lookup-smem*category
   (state <s> ^name find-object
              ^nounjective <nj>
              ^ask-user <ask>)
   (<nj> -^category
          ^word <word>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name lookup-smem
        ^category smem
        ^ask-user <ask>
        ^lookup-type category
        ^query <q>
        ^desired.category true)
   (<q> ^type word2cat ^word <word> ^category <cat>)
}

# Successful retrieval
sp {find-object*apply*lookup-smem*category*success
   (state <s> ^name find-object
              ^operator <o>
              ^nounjective <nj>)
   (<o> ^name lookup-smem
        ^lookup-type category
        ^query.word <word>
        ^result {<cat> <> failure})
   (<nj> ^word <word>)
-->
   (<nj> ^category <cat>)
}

# Successful retrieval
sp {find-object*apply*lookup-smem*category*failure
   (state <s> ^name find-object
              ^operator <o>
              ^nounjective <nj>)
   (<o> ^name lookup-smem
        ^lookup-type category
        ^query.word <word>
        ^result failure)
   (<nj> ^word <word>)
-->
   (<nj> ^category failure)
}