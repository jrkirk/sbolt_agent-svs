# For each attribute-value pair it proposes find-value-group
# When completed, it adds ^processed.<type> <val> to the superstate
sp {propose*find-value-group
   (state <s> ^name comprehend-att
             -^processed.<att-type> <val>
              ^att-to-comp <att>
              ^att-type <att-type>
              ^object.mapping.att-val-pair <av-pair>)
   (<av-pair> ^att.<att-type> <att>
              ^val.<att-type> <val>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name find-value-group
        ^category object-processing
        ^value <val>)
}