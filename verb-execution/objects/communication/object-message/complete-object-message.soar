sp {object-message*propose*complete-object-message
   (state <s> ^name object-message)
-->
   (<s> ^operator <o> +)
   (<o> ^name complete-object-message
        ^category object-processing)
}

# Mark the action performed in the interaction stack
sp {object-message*apply*complete-object-message
   (state <s> ^name object-message
              ^operator.name complete-object-message
              ^topstate.status <status>)
-->
   (<status> ^performed.learning-event object)
}