sp {prefer*find-relations*over*lookup-preposition*describe-message
   (state <s> ^name describe-message
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name find-relations)
   (<o2> ^name lookup-preposition)
-->
   (<s> ^operator <o2> > <o1>)
}

sp {prefer*find-relations*over*describe-object*describe-message
   (state <s> ^name describe-message
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name find-relations)
   (<o2> ^name describe-object)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {prefer*find-relations*over*complete-describe-message*describe-message
   (state <s> ^name describe-message
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name find-relations)
   (<o2> ^name complete-describe-message)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {prefer*lookup-preposition*over*describe-object*describe-message
   (state <s> ^name describe-message
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name lookup-preposition)
   (<o2> ^name describe-object)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {prefer*describe-object*over*complete-describe-message*describe-message
   (state <s> ^name describe-message
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name describe-object)
   (<o2> ^name complete-describe-message)
-->
   (<s> ^operator <o1> > <o2>)
}